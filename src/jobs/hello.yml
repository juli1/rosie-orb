description: >
  Run Datadog analysis and upload results
# What will this job do?

executor: default

parameters:
  to:
    type: string
    default: "World"
    description: "Hello to whom?"
steps:
  - greet:
      to: << parameters.to >>
